/** Opt-in in-memory text diagnostics; never render data as markup. */
class DebugStream {
 constructor({limit=200}={}){this.limit=limit;this.enabled=false;this.rows=[];this.panel=document.getElementById('debug-stream');this.list=document.getElementById('debug-lines');this.button=document.getElementById('control-debug');this.button?.addEventListener('click',()=>{this.enabled=!this.enabled;this.button.setAttribute('aria-pressed',String(this.enabled));this.panel.hidden=!this.enabled;});document.getElementById('debug-clear')?.addEventListener('click',()=>{this.list.replaceChildren();this.rows=[];this.last=null;});window.nodie?.onDebugEvent?.(event=>this.add(event.source,event.text));}
 add(source,text,append=false){if(!this.enabled || typeof source!=='string'||typeof text!=='string'||!text.trim())return;source=source.slice(0,40);text=text.slice(0,2000);const now=Date.now();if(append&&this.last?.source===source&&now-this.last.at<3000&&this.last.text.length+text.length<2000){this.last.text+=' '+text;this.last.at=now;this.last.node.textContent=this.last.prefix+this.last.text;}else{const node=document.createElement('li');const prefix=new Date(now).toLocaleTimeString()+' · '+source+' · ';node.textContent=prefix+text;this.list.append(node);this.last={node,prefix,text,source,at:now};this.rows.push(node);if(this.rows.length>this.limit)this.rows.shift().remove();}this.panel.scrollTop=this.panel.scrollHeight;}
 event(data){if(data.type==='conversation.item.input_audio_transcription.delta')this.add('Heard',data.delta,true);if(data.type==='response.text.delta')this.add('Said',data.delta,true);if(data.type==='input_audio_buffer.speech_started')this.add('Microphone','Speech detected');if(data.type==='response.created')this.last=null;}
}
if(typeof window!=='undefined'){window.DebugStream=DebugStream;window.addEventListener('DOMContentLoaded',()=>{if(window.NodieRenderer)window.NodieRenderer.debugStream ||= new DebugStream();});}
if(typeof module!=='undefined')module.exports={DebugStream};
